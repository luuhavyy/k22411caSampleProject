<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".EmployeeManagementActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingBottom="12dp"
            android:text="@string/title_employeemanagement_title"
            android:textAlignment="center"
            android:textSize="48sp"
            android:textStyle="bold" />

        <!-- Table hiển thị danh sách Employee -->
        <TableLayout
            android:id="@+id/tblEmployees"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:stretchColumns="*">

            <!-- Header Row -->
            <TableRow android:layout_height="match_parent">

                <TextView
                    android:text="@string/title_employeemanagement_id"
                    android:textSize="36sp"
                    android:textStyle="bold" />

                <TextView
                    android:text="@string/title_employeemanagement_name"
                    android:textSize="36sp"
                    android:textStyle="bold" />

                <TextView
                    android:text="@string/title_employeemanagement_position"
                    android:textSize="36sp"
                    android:textStyle="bold" />
            </TableRow>

            <!-- Các dòng dữ liệu sẽ được thêm từ Java bằng code -->
        </TableLayout>
    </LinearLayout>
</ScrollView>
